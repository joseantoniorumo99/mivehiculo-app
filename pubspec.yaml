name: mivehiculo
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.4.1+6

environment:
  sdk: ^3.13.3

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # Fechas y selectores en espanol.
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # El Bluetooth CLASICO (SPP/RFCOMM), que es la razon de que esta app exista:
  # ningun navegador puede hablar con un ELM327 corriente. MIT, no contagia.
  flutter_classic_bluetooth: ^1.4.0

  # Guardado local. Primero local, backend despues.
  shared_preferences: ^2.5.5
  path_provider: ^2.1.6

  # La foto de la factura, del movil a la carpeta privada de la app.
  image_picker: ^1.2.3

  # El mapa. flutter_map es BSD y pinta teselas de quien se le diga; no obliga
  # a ninguna clave ni a ninguna tarjeta, que es lo que descarto a Google.
  flutter_map: ^8.3.2
  latlong2: ^0.10.1

  # Los sitios salen de nuestra propia funcion en Cloudflare, no de Overpass
  # directamente: Overpass contesta 406 a cualquier User-Agent de navegador.
  http: ^1.6.0
  geolocator: ^14.0.3
  url_launcher: ^6.3.2

  # La cuenta y la copia en la nube. Mismo proyecto de Appwrite que la web:
  # el coche que das de alta en el movil sale en el ordenador y al reves.
  appwrite: ^26.2.0

  # Compartir el expediente (a WhatsApp, al correo, a quien compre el coche).
  share_plus: ^13.3.0

  # Lectura del OBD en segundo plano: un trabajo de WorkManager que Android
  # ejecuta cuando toca y mata cuando quiere. Sin servicio permanente.
  workmanager: ^0.10.10

  # Autoactualizacion: saber que version soy y abrir el APK descargado.
  package_info_plus: ^10.2.1
  open_filex: ^4.7.0

  # El informe completo del coche en PDF (tipo historial de compraventa), hecho
  # en el propio movil y compartido desde el. `printing` solo pone la hoja de
  # compartir/imprimir del sistema; no manda nada a ningun servidor.
  pdf: ^3.11.0
  printing: ^5.14.0

  # Avisos locales: "el taller ha confirmado tu cita", "te ha mandado el
  # informe". Locales de verdad: los pone la propia app al sincronizar, sin
  # servicio de notificaciones push de pago.
  flutter_local_notifications: ^19.0.0

  # Leer la ficha tecnica de una foto, en el propio movil y sin conexion. El
  # bastidor y la matricula no salen del telefono. Modelo latino incluido.
  google_mlkit_text_recognition: ^0.17.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  assets:
    # Matriculaciones reales en Espana de la Agencia Europea de Medio Ambiente.
    # Va DENTRO de la app y no se pide por red: son 99 KB y asi dar de alta un
    # coche funciona en un aparcamiento sin cobertura.
    - assets/catalogo.json
    # La letra del informe PDF (Roboto, Apache 2.0). Las tipografias de serie
    # del PDF no tienen el simbolo del euro ni todas las tildes.
    - assets/fuentes/Roboto-Regular.ttf
    - assets/fuentes/Roboto-Bold.ttf

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
