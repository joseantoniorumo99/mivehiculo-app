<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Red: los sitios del mapa, las teselas, la copia en la nube y la
         descarga de versiones nuevas. Flutter solo la declara en depuración;
         en release hay que ponerla. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Ubicación: para medir las distancias DESDE la persona y centrar el
         mapa. Sin ella el mapa funciona igual, centrado a mano.

         OJO CON `tools:node="replace"`: el plugin de Bluetooth declara estos
         dos permisos con `maxSdkVersion="30"` (solo los necesita en Android
         viejo), y al fusionar los manifiestos ESA versión ganaba. Resultado:
         en Android 12+ el permiso no existía, el sistema nunca preguntaba y
         el mapa salía sin ubicación sin explicación. Con `replace`, manda la
         declaración de la app, sin tope de versión. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" tools:node="replace"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" tools:node="replace"/>

    <!-- Instalar la versión nueva que la propia app descarga. El sistema
         pide confirmación la primera vez ("permitir instalar de esta app"). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <application
        android:label="Mi Vehículo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Cuando el móvil se conecta por Bluetooth al COCHE (el manos
             libres o la radio), Android despierta este receptor aunque la app
             esté cerrada: es de los pocos avisos que el sistema sigue
             entregando a apps dormidas. El receptor encola una lectura del
             OBD para dentro de un minuto y se vuelve a dormir. Cero consumo
             mientras no conduces. -->
        <receiver
            android:name=".ReceptorBluetooth"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED"/>
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Desde Android 11 hay que declarar qué apps de fuera se quieren abrir:
         llamar al taller, abrir su web, cómo llegar, y el procesado de texto
         que usa el propio motor de Flutter. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
    </queries>
</manifest>
